version: '3.8'

services:
  react-native-calculator:
    build: .
    ports:
      - "8081:8081"
    volumes:
      - .:/app
      - /app/node_modules
    command: npx react-native start --host 0.0.0.0